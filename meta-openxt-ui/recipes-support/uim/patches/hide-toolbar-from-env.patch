Upstream-Status: Inappropriate [OpenXT]

--- a/gtk2/toolbar/standalone-gtk.c	2026-08-20 10:52:05.666829365 -0400
+++ b/gtk2/toolbar/standalone-gtk.c	2026-08-20 10:52:05.678829365 -0400
@@ -246,6 +246,9 @@
   GtkWidget *frame;
 #endif /* OPENXT_BRANDING */
 
+  const char *env_show_toolbar = getenv("UIM_SHOW_TOOLBAR");
+  int show_toolbar = !env_show_toolbar || !strcmp(env_show_toolbar, "1");
+
   setlocale(LC_ALL, "");
   bindtextdomain(PACKAGE, LOCALEDIR);
   textdomain(PACKAGE);
@@ -311,6 +314,9 @@
   g_signal_connect(G_OBJECT(window), "size-request",
 		   G_CALLBACK(size_request_cb), NULL);
 
+  if (!show_toolbar)
+    goto skip_toolbar;
+
   gtk_widget_show_all(GTK_WIDGET(window));
 
 #ifdef OPENXT_BRANDING
@@ -351,6 +357,7 @@
     helper_win_set_position(window, x, y);
   }
 
+skip_toolbar:
   gtk_main();
 
   uim_quit();
