Upstream-Status: Inappropriate [OpenXT]

Meson 1.x i18n.merge_file() no longer accepts a positional target name.

--- a/meson.build
+++ b/meson.build
@@ -253,8 +253,7 @@
 desktop_file_validate = find_program('desktop-file-validate', required: false)
 
 foreach desktop: desktop_files
-  i18n.merge_file(
-    desktop + '-desktop',
+  i18n.merge_file(
     input: desktop + '.desktop.in',
     output: desktop + '.desktop',
     install: true,
@@ -275,8 +274,7 @@
 appdata = 'nm-connection-editor.appdata.xml'
 
 i18n.merge_file(
-  'desktop',
-  input: appdata + '.in',
+  input: appdata + '.in',
   output: appdata,
   install: true,
   install_dir: join_paths(nma_datadir, 'metainfo'),
