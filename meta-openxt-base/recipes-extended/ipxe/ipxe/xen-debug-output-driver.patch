Upstream-Status: Inappropriate [OpenXT]

diff -Naur a/config/config.c b/config/config.c
--- a/config/config.c	2026-08-17 20:58:34.724189296 -0400
+++ b/config/config.c	2026-08-17 20:58:34.820189295 -0400
@@ -104,6 +104,9 @@
 #ifdef PXE_STACK
 REQUIRE_OBJECT ( pxe_call );
 #endif
+#ifdef CONSOLE_XEN
+REQUIRE_OBJECT ( xen_console );
+#endif
 
 /*
  * Drag in all requested download protocols
diff -Naur a/config/console.h b/config/console.h
--- a/config/console.h	2026-08-17 20:58:34.724189296 -0400
+++ b/config/console.h	2026-08-17 20:58:34.840189295 -0400
@@ -54,6 +54,7 @@
 
 /* Console types supported only on x86 CPUs */
 #if defined ( __i386__ ) || defined ( __x86_64__ )
+  #define CONSOLE_XEN		/* Xen debug console port. */
   //#define CONSOLE_DEBUGCON	/* Bochs/QEMU/KVM debug port console */
   //#define CONSOLE_DIRECT_VGA	/* Direct access to VGA card */
   //#define CONSOLE_PC_KBD	/* Direct access to PC keyboard */
diff -Naur a/core/xen_console.c b/core/xen_console.c
--- a/core/xen_console.c	1969-12-31 19:00:00.000000000 -0500
+++ b/core/xen_console.c	2026-08-17 20:58:34.844189295 -0400
@@ -0,0 +1,20 @@
+#include <ipxe/init.h>
+#include <ipxe/console.h>
+#include <ipxe/io.h>
+
+/** @file
+ *
+ * Xen console
+ *
+ */
+
+struct console_driver xen_console __console_driver;
+
+void xen_putc ( int ch ) {
+        outb(ch, 0xe9);
+}
+
+
+struct console_driver xen_console __console_driver = {
+.putchar = xen_putc,
+};
