Upstream-Status: Inappropriate [OpenXT: passwd/shadow live under /config/etc]
Subject: [PATCH] pam_unix: use /config/etc for passwd/shadow (OpenXT)

OpenXT keeps passwd/shadow on the config partition, mounted at
/config/etc (with /etc/passwd as a symlink). Redirect pam_unix
paths accordingly for password changes and lock files.

diff -ruN a/modules/pam_unix/lckpwdf.-c b/modules/pam_unix/lckpwdf.-c
--- a/modules/pam_unix/lckpwdf.-c	2026-01-22 03:00:00.000000000 -0500
+++ b/modules/pam_unix/lckpwdf.-c	2026-08-17 17:33:51.496264266 -0400
@@ -30,7 +30,7 @@
 #include <selinux/selinux.h>
 #endif
 
-#define LOCKFILE "/etc/.pwd.lock"
+#define LOCKFILE "/config/etc/.pwd.lock"
 #define TIMEOUT 15
 
 static int lockfd = -1;
@@ -67,7 +67,7 @@
 			char *create_context_raw;
 			int rc;
 
-			if(getfilecon_raw("/etc/passwd", &create_context_raw))
+			if(getfilecon_raw("/config/etc/passwd", &create_context_raw))
 				return -1;
 			rc = setfscreatecon_raw(create_context_raw);
 			freecon(create_context_raw);
diff -ruN a/modules/pam_unix/passverify.c b/modules/pam_unix/passverify.c
--- a/modules/pam_unix/passverify.c	2026-01-22 03:00:00.000000000 -0500
+++ b/modules/pam_unix/passverify.c	2026-08-17 17:33:51.500264266 -0400
@@ -336,9 +336,9 @@
 
 /* passwd/salt conversion macros */
 
-#define PW_TMPFILE              "/etc/.pam.passwdXXXXXX"
-#define SH_TMPFILE              "/etc/.pam.shadowXXXXXX"
-#define OPW_TMPFILE             SCONFIG_DIR "/.pam.opasswdXXXXXX"
+#define PW_TMPFILE              "/config/etc/.pam.passwdXXXXXX"
+#define SH_TMPFILE              "/config/etc/.pam.shadowXXXXXX"
+#define OPW_TMPFILE             "/config/etc/security/.pam.opasswdXXXXXX"
 
 /*
  * fmkstemp - creates and opens a temporary file from given pattern
@@ -542,7 +542,7 @@
 {
     static int confined = -1;
     int fd;
-    char tempfile[]="/etc/.pwdXXXXXX";
+    char tempfile[]="/config/etc/.pwdXXXXXX";
 
     if (confined != -1)
 	return confined;
@@ -554,7 +554,7 @@
     }
 
     /* let's try opening shadow read only */
-    if ((fd=open("/etc/shadow", O_RDONLY | O_CLOEXEC)) != -1) {
+    if ((fd=open("/config/etc/shadow", O_RDONLY | O_CLOEXEC)) != -1) {
         close(fd);
         confined = 0;
         return confined;
@@ -675,7 +675,7 @@
 #ifdef WITH_SELINUX
     if (SELINUX_ENABLED) {
       char *passwd_context_raw = NULL;
-      if (getfilecon_raw("/etc/passwd",&passwd_context_raw)<0) {
+      if (getfilecon_raw("/config/etc/passwd",&passwd_context_raw)<0) {
         return PAM_AUTHTOK_ERR;
       };
       if (getfscreatecon_raw(&prev_context_raw)<0) {
@@ -835,7 +835,7 @@
 #ifdef WITH_SELINUX
     if (SELINUX_ENABLED) {
       char *passwd_context_raw = NULL;
-      if (getfilecon_raw("/etc/passwd",&passwd_context_raw)<0) {
+      if (getfilecon_raw("/config/etc/passwd",&passwd_context_raw)<0) {
 	return PAM_AUTHTOK_ERR;
       };
       if (getfscreatecon_raw(&prev_context_raw)<0) {
@@ -856,7 +856,7 @@
       goto tmp_error;
     }
 
-    opwfile = fopen("/etc/passwd", "re");
+    opwfile = fopen("/config/etc/passwd", "re");
     if (opwfile == NULL) {
 	fclose(pwfile);
 	err = 1;
@@ -917,7 +917,7 @@
 
 done:
     if (!err) {
-	if (!rename(tempfile, "/etc/passwd"))
+	if (!rename(tempfile, "/config/etc/passwd"))
 	    pam_syslog(pamh,
 		LOG_NOTICE, "password changed for %s", forwho);
 	else
@@ -955,7 +955,7 @@
 #ifdef WITH_SELINUX
     if (SELINUX_ENABLED) {
       char *shadow_context_raw = NULL;
-      if (getfilecon_raw("/etc/shadow",&shadow_context_raw)<0) {
+      if (getfilecon_raw("/config/etc/shadow",&shadow_context_raw)<0) {
 	return PAM_AUTHTOK_ERR;
       };
       if (getfscreatecon_raw(&prev_context_raw)<0) {
@@ -976,7 +976,7 @@
 	goto tmp_error;
     }
 
-    opwfile = fopen("/etc/shadow", "re");
+    opwfile = fopen("/config/etc/shadow", "re");
     if (opwfile == NULL) {
 	fclose(pwfile);
 	err = 1;
@@ -1057,7 +1057,7 @@
 
  done:
     if (!err) {
-	if (!rename(tempfile, "/etc/shadow"))
+	if (!rename(tempfile, "/config/etc/shadow"))
 	    pam_syslog(pamh,
 		LOG_NOTICE, "password changed for %s", forwho);
 	else
diff -ruN a/modules/pam_unix/passverify.h b/modules/pam_unix/passverify.h
--- a/modules/pam_unix/passverify.h	2026-01-22 03:00:00.000000000 -0500
+++ b/modules/pam_unix/passverify.h	2026-08-17 17:33:51.496264266 -0400
@@ -9,7 +9,7 @@
 
 #define PAM_UNIX_RUN_HELPER PAM_CRED_INSUFFICIENT
 
-#define OLD_PASSWORDS_FILE      SCONFIG_DIR "/opasswd"
+#define OLD_PASSWORDS_FILE      "/config/etc/opasswd"
 
 int
 is_pwd_shadowed(const struct passwd *pwd);
diff -ruN a/modules/pam_unix/support.c b/modules/pam_unix/support.c
--- a/modules/pam_unix/support.c	2026-01-22 03:00:00.000000000 -0500
+++ b/modules/pam_unix/support.c	2026-08-17 17:33:51.500264266 -0400
@@ -357,7 +357,7 @@
 	if (!matched && files && strchr(name, ':') == NULL) {
 		FILE *passwd;
 
-		passwd = fopen("/etc/passwd", "re");
+		passwd = fopen("/config/etc/passwd", "re");
 		if (passwd != NULL) {
 			size_t n = 0, userlen;
 			ssize_t r;
