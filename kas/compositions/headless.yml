# Headless product: meta-openxt-base only (dom0 / installer / stubdomain).
# Residual: meta-qt5 + meta-vglass remain via common/base.yml for IVC/stubdomain.
header:
  version: 14
  includes:
    - kas/common/base.yml
    - kas/common/conf.yml
repos:
  xenclient-oe:
    layers:
      meta-openxt-base:
